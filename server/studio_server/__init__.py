"""tesserae-studio thin local server.

Reverse-proxies a running Tesserae so the browser stays single-origin (see
``docs/architecture.md``) and hosts Studio's own API under ``/studio/api``.
"""

__version__ = "0.1.0"
